[package]
name = "nixie-wire"
version = "0.1.0"
edition = "2024"

[dependencies]
defmt = { version = "1.0.1", optional = true }
serde = { version = "1", default-features = false, features = ["derive"] }

[features]
defmt = ["dep:defmt"]

[dev-dependencies]
postcard = "1"
