\documentclass[11pt,letterpaper]{article}

% ---------------------------------------------------------------------------
% Nixie Clock -- Design Analysis
%
% Derives the board's key electrical characteristics from first principles
% and from component datasheets on file in docs/datasheets/. This document
% works at the schematic level only; it is the source from which the
% eventual Hardware User Guide's tables are extracted.
% ---------------------------------------------------------------------------

\usepackage[letterpaper,margin=1in]{geometry}

% Project-local font (PT Serif: Latin + Cyrillic, OFL-licensed, committed
% under fonts/ so the build has no system-font dependency). Requires
% XeLaTeX or LuaLaTeX -- fontspec does not work under pdfLaTeX.
\usepackage{fontspec}
\setmainfont{PTSerif}[
  Path = fonts/,
  Extension = .ttf,
  UprightFont = *-Regular,
  BoldFont = *-Bold,
  ItalicFont = *-Italic,
  BoldItalicFont = *-BoldItalic,
]

\usepackage{amsmath,amssymb}
\usepackage{siunitx}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{graphicx}
\usepackage{enumitem}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}
\usepackage{cleveref}

\sisetup{
  per-mode = symbol,
  detect-all = true,
  table-align-text-post = false,
}

% Consistent units used throughout
\DeclareSIUnit\decade{decade}

% Shorthand for a datasheet citation inside table cells and prose.
% Usage: \ds{SN74141\_TI.pdf}{2}
\newcommand{\ds}[2]{\textit{#1}, p.~#2}

\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}

% \DesignRev is generated by scripts/make_release.py --sync-doc-rev from
% the KiCad project's REV text variable -- do not hand-edit revision.tex.
\input{revision}

\title{Nixie Clock --- Design Analysis}
\author{}
\date{Rev \DesignRev{} --- compiled \today}

\begin{document}
\maketitle

\begin{abstract}
\noindent
This document derives the key electrical characteristics of the Nixie
Clock board from the datasheets of its major components and from
first-principles circuit analysis. The analysis stays at the
schematic level: values follow from component ratings and circuit
topology rather than from a specific PCB footprint, except where a
part is inseparable from one particular package. Every numeric claim
about a named, design-driving component traces to a page in a
datasheet under \texttt{docs/datasheets/}; anything not covered by a
datasheet there is sized to standard engineering practice instead. The
document proceeds subsystem by subsystem, working backward from the
load: the Nixie tubes and their binary-coded-decimal (BCD)
decoder/drivers set the
requirements for the 170~V anode supply and for the 5~V rail that
powers those same drivers, the ESP32 MCU sets the requirements for the
3.3~V rail, and all three converters together set the requirements
for the USB-PD front end that supplies them.
\end{abstract}

\tableofcontents

\input{sections/01_introduction}
\input{sections/02_tubes}
\input{sections/03_bcd_driver}
\input{sections/04_hv_components}
\input{sections/05_hv_converter}
\input{sections/06_lv_components}
\input{sections/07_5v_converter}
\input{sections/08_3v3_converter}

\end{document}
